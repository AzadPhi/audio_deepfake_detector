# setup.py
from setuptools import setup

setup(name='preprocess',
      description="package permettant de transformer des sons en np array et plots",
      packages=["sound_prep"]) # You can have several packages, try it
